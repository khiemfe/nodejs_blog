{{!-- <div class="search-form mt-4 mb-4">
    <form method="GET" action="/news"> 
        <div class="form-group">
            <label for="search-input">Từ khoá</label>
            <input type="text" name="fullname">
            <input type="text" name="age">
            <input type="text" name="q" class="form-control" id="search-input" placeholder="Nhập từ khoá">
        </div>
        <button  type="submit" class="btn btn-primary mt-2">Tìm kiếm</button>
    </form>
</div> --}}

{{!-- 
    method là submit với phương thức nào
    action là sunmit ở đâu (ko có hoặc để trống thì nó sẽ tới url mặc định)
--}}

<div class="search-form mt-4 mb-4">
    <form method="POST" action=""> 
        <div class="form-group">
            <label for="search-input">Từ khoá</label>
            <input type="text" name="q" class="form-control" id="search-input" placeholder="Nhập từ khoá">
        </div>

         <div class="form-group">
            <label for="gender-input">Từ khoá</label>
            <select type="gender" name="gender" class="form-control" id="gender-input" placeholder="Nhập từ khoá">
                <option value="">Chọn giới tính</option>
                <option value="male">Nam</option>
                <option value="female">Nữ</option>
            </select>
        </div>

        <button  type="submit" class="btn btn-primary mt-2">Tìm kiếm</button>
    </form>
</div>
